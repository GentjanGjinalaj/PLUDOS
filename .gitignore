# Python
pludos_venv/
__pycache__/
*.py[cod]
*.egg-info/
build_pludos_dashboard.py

# Data files (runtime output, never commit)
*.parquet
# ram_buffer/ directories hold .parquet files (ignored above) and a .gitkeep — don't ignore the dir

# Trained global models — written by server.py _persist_global_model after each FL round.
server/models/

# Secrets and local config (never commit)
# Catch-all: any .env file anywhere in the tree (root, client/, server/, etc.)
**/.env
**/.env.*
**/wifi_credentials.h

# STM32CubeIDE build output
STM_Shuttles/PLUDOS_Edge_Node/Debug/
*.elf
*.bin
*.hex
*.map
*.list
*.o
*.d

# AI assistant context files (project-specific, not for repo)
GEMINI.md
CLAUDE.md
client/CLAUDE.md
server/CLAUDE.md
STM_Shuttles/CLAUDE.md


.github/
.vscode/

# Root-level ops files — local use only (contain credentials / stale content)
OPERATIONS.md
QUICKSTART.md

# Docs: only block the sensitive or internal-only files.
# Technical reference docs (architecture, wire protocol, etc.) are intentionally public.
docs/DOWNLOAD.md
docs/DEPLOYMENT_CHECKLIST.md
docs/THESIS_KEYPOINTS.md
docs/QUICK_REFERENCE.md
docs/JETSON_DEPLOYMENT.md
docs/current_problems.md
docs/next_steps.md
docs/future_options.md
docs/to-do.md
docs/to-do_results.md
# Log directories — auto-created at runtime, never committed.
# server.py creates logs/ via mkdir(parents=True, exist_ok=True).
# entrypoint.sh creates client/logs/alumet/ via mkdir -p.
logs/
client/logs/

# Claude Code worktrees (isolation branches, not for repo)
.claude/worktrees/
.claude/



