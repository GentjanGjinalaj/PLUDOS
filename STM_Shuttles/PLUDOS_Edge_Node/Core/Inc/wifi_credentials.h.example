/* wifi_credentials.h.example — committed template.
 * Copy to wifi_credentials.h (gitignored) and fill in real values before building. */
#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

#define WIFI_SSID     "your_ssid_here"
#define WIFI_PASSWORD "your_password_here"
#define JETSON_IP     "0.0.0.0"          /* Tailscale IP of the gateway Jetson */
#define SHUTTLE_ID    "STM32-Alpha"      /* 12-char max, null-padded; must be unique per shuttle */

#endif /* WIFI_CREDENTIALS_H */
