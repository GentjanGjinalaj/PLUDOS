# Optional for VPN profile only (Jetson <-> Server relay).
# STM32 <-> Jetson CoAP/UDP local communication does NOT require this key.
# Leave empty if running only data-engine for local ingestion tests.
TS_AUTHKEY=
