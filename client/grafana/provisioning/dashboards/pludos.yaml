apiVersion: 1

# Loads any dashboard JSON dropped into /dashboards (bind-mounted from
# client/grafana/dashboards). Matches the server-side provider layout.
providers:
  - name: PLUDOS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /dashboards
