# Jetson Orin Nano (aarch64) — JetPack 6 (R36), Python 3.10, python:3.10-slim base.
# Use loose pins (>=) so pip resolves the latest wheel available for aarch64/py3.10.
# To regenerate a locked file: pip freeze > requirements.txt  (with Jetson venv active)
#
# data-engine.py needs: influxdb-client, pandas, pyarrow, psutil
# client.py needs:      flwr, xgboost, numpy, scikit-learn, grpcio
#
influxdb-client>=1.36
pandas>=2.1
pyarrow>=14.0        # zstd codec included in pyarrow wheels — no extra dep needed
psutil>=5.9          # RAM introspection for buffer-limit startup log and auto mode
flwr>=1.10
xgboost>=1.7
numpy>=1.24
scikit-learn>=1.3
grpcio>=1.50
grpcio-health-checking>=1.50
